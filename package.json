{
  "name": "repo-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal Render-deployable service that will later bridge ChatGPT specs to GitHub operations.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "helmet": "^7.1.0"
  }
}
