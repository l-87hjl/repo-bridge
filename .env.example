# repo-bridge Environment Variables
# Copy this file to .env and fill in your values

# =============================================================================
# REQUIRED: GitHub App Configuration
# =============================================================================

# Your GitHub App's ID (found in your App settings)
GITHUB_APP_ID=123456

# Your GitHub App's private key in PEM format
# When deploying to Render or other platforms, you can paste the entire key
# with literal \n characters - the service will automatically convert them
GITHUB_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\nMIIE...\n-----END RSA PRIVATE KEY-----"

# =============================================================================
# OPTIONAL: GitHub Configuration
# =============================================================================

# Default GitHub App installation ID
# If not set, you must provide installationId in each request
# Find this in your App's installations settings
GITHUB_INSTALLATION_ID=12345678

# Default branch for commits (defaults to 'main')
DEFAULT_BRANCH=main

# =============================================================================
# OPTIONAL: Security Configuration
# =============================================================================

# API authentication token
# If set, all /apply and /github/dryrun requests must include:
#   Authorization: Bearer <token>
# Leave unset to allow unauthenticated requests (not recommended for production)
API_AUTH_TOKEN=your-secret-token-here

# =============================================================================
# OPTIONAL: Allowlists (comma-separated)
# =============================================================================

# Allowed repositories (comma-separated, lowercase)
# Supports wildcards: "myorg/*" allows all repos in myorg
# Leave unset to allow all repositories
# Examples:
#   ALLOWED_REPOS=myorg/myrepo
#   ALLOWED_REPOS=myorg/*,otherorg/specific-repo
ALLOWED_REPOS=

# Allowed file paths (comma-separated)
# Supports wildcards and prefix matching
# Leave unset to allow all paths
# Examples:
#   ALLOWED_PATHS=src/*,docs/*
#   ALLOWED_PATHS=config/,data/
ALLOWED_PATHS=

# =============================================================================
# OPTIONAL: Server Configuration
# =============================================================================

# Server port (defaults to 3000)
PORT=3000
